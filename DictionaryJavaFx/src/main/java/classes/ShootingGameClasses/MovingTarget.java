package classes.ShootingGameClasses;

public class MovingTarget extends Target {
    public MovingTarget(Color color, String showText, int score) {
        super(color, showText, score);
    }

    @Override
    public void move() {

    }
}
